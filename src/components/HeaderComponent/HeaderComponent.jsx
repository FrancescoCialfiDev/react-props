
// Create a function wich return a component with an html code for the main part of the page.

function Header() {
    return (
        <header className="text-center p-3 border">
            <h1>Il mio blog</h1>
        </header>
    )
}

export default Header; // Export the component for import into the app.jsx file.